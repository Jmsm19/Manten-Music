import styled from 'styled-components';

const StyledFooter = styled.footer`
  padding: 10px;
  font-size: 2rem;
  text-align: center;
  p {
    color: var(--gray);
  }
`;

export default StyledFooter;
